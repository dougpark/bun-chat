{
  "name": "bun-chat",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun bunfile.ts",
    "dev": "bun run build && bun --hot src/server.ts",
    "start": "bun run build && bun src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
